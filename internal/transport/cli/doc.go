// Package cli will contain the console transport layer.
//
// The CLI should call the same service layer as HTTP handlers do, differing
// only in argument parsing and output formatting.
package cli
